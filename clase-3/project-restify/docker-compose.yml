version: '3.1'
services:
   mongo:
     image: mongo:4.4
     restart: always
     ports:
       - 27017:27017
     environment:
       MONGO_INITDB_ROOT_USERNAME: emi
       MONGO_INITDB_ROOT_PASSWORD: krowdy

## para ejecutar: docker-compose up -d
